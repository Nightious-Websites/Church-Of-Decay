# Claude Code dev tooling
.claude/
CLAUDE.md
AGENTS.md

# Codex CLI marker
.codex

# Playwright MCP session artifacts
.playwright-mcp/

# Verification screenshots (taken into repo root during UI iteration)
/*.png
/*.jpeg

# Superpowers brainstorm artifacts
.superpowers/

# Historical planning docs (React-era handoff briefs — kept locally, not tracked)
docs/

# Node / Vite build artifacts
node_modules/
dist/
.vite/

# Legacy pre-migration snapshot (kept locally for reference during port)
index.legacy.html
